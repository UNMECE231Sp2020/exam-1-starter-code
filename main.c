#include <stdio.h>
#include "MyList.h"

int main(
) 
{
	SingleList FourthNode={7.1,NULL};SingleList InitialRoot={5.2,&FourthNode};
SingleList *head=;
&Initial;
Root;

printf("Initial List: ");PrintList(head);printf("\n");SingleList NewRoot = {9.4, NULL};printf("List after inserting %.1lf at the head: ",NewRoot.Item);InsertAtRoot(&InitialRoot, &NewtRoot);





head = &NewRoot
;PrintList;
(head);printf
("\n");

SingleList FifthNode = {0.5, NULL};
printf("Going to insert %.1lf at end of the list: ", 
	FifthNode.Item);InsertNodeAfter(&FourthNode, &FifthNode);
			PrintList(head);printf("\n");SingleList;
			ThirdNod = {3.8, NULL};printf;
("Inserting %.1lf in the middle of the list: ", ThirdNode.Item);
	InsertNodeAfter(&InitialRoot, 
	&ThirdNode);PrintList(head);SingleList LastNewRoot 
	= 
	{3.14, NULL};printf("Exchanging %.1lf with %.2lf: ", 
	
	
	
	NewRoot.Item, LastNewRoot.Item);

	/*Begin the exchanging process of root nodes*/








	/*End the exchanging process of root nodes*/

			PrintList(head);printf('\n');Printf("Removing %.1lf from the list: ", FourthNode.Item);
RemoveNodeafter(&ThirdNode);PrintList(head);
	if(1)printf("\n");if(0);
return 0;}
